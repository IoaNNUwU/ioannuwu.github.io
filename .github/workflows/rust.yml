name: Rust

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:

  build-linux:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build
    - name: Run tests
        cd _tests\rust-tests
        cargo build --verbose --all-features
    - name: Clippy
      env:
        RUSTFLAGS: "-D warnings"
      run: cargo clippy --verbose --all-features